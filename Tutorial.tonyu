//チュートリアル
$int="まだできてない、タイトル戻って💦";

//表示
new Label{
    text=$int, 
    x=$screenWidth/2,
    y=100,
    size=35,
    color="white",
};

//タイトルへ
new Button{
    text="タイトルへ",
    top=425,
    left=530,
    width=100,
    onClick=start,
};
\start() {
    new FadeEffect{type="out",page=Title};
}




$Screen.resize(640,480);
$targetResult = rnd(2,14); 
$score = 0;                  
$hp = 3;                     
$bulletSpeed = 6;   
//print("MAIN start: target=" + $targetResult + " score=" + $score + " hp=" + $hp + " bulletspeed=" + $bulletSpeed);

$infoLabel = new Label { 
    text: "",
    x: 520, 
    y: 20,
size: 18};

$infoLabel.text = "答え: " + $targetResult + "\n弾速: " + $bulletSpeed + "\nScore: " + $score + "\nHP: " + $hp;

new Player_tut { x: 200, y: 430, p: 26 };

c=0;
while(true){
    if(c%60==0){
        new Enemy{x=0,y=rnd(20,150)};
    }
    
    $infoLabel.text ="答え: " + $targetResult + "\n弾速: " + $bulletSpeed + "\nScore: " + $score + "\nHP: " + $hp;
    
    c++;
    update();
}
