//チュートリアル
//ちゃんとゲームが完成したらコピペして中の設定いじって無敵時間無制限版にする
$int="まだできてない、タイトル戻って💦";

//表示
new Label{
    text=$int, 
    x=$screenWidth/2,
    y=100,
    size=35,
    color="white",
};

//タイトルへ
new Button{
    text="タイトルへ",
    top=425,
    left=530,
    width=100,
    onClick=start,
};
\start() {
    new FadeEffect{type="out",page=Title};
}




print("MAIN start: target=" + $targetResult + " score=" + $score + " hp=" + $hp + " bulletspeed=" + $bulletSpeed);
$Screen.resize(640,480);
$targetResult = rnd(2, 19);  
$score = 0;                  
$hp = 3;                     
$bulletSpeed = 6;            

$infoLabel = new Label { 
    text: "",
    x: 520, 
    y: 20,
    size: 18,
    color: "white"
};
$infoLabel.text = "答え: " + $targetResult + "\n弾速: " + $bulletSpeed + "\nScore: " + $score + "\nHP: " + $hp;

new Player { x: 200, y: 430, p: 27 };

c=0;
while(true){
    if(c%35==0){
        new Enemy{x=0,y=rnd(20,150)};
    }
    
    $infoLabel.text ="答え: " + $targetResult + "\n弾速: " + $bulletSpeed + "\nScore: " + $score + "\nHP: " + $hp;
    
    c++;
    update();
}



